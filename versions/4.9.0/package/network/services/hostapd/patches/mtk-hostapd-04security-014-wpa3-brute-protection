diff --git a/src/ap/ieee802_11.c b/src/ap/ieee802_11.c
index 892ad7f..76d234b 100644
--- a/src/ap/ieee802_11.c
+++ b/src/ap/ieee802_11.c
@@ -1655,6 +1655,7 @@ static void handle_auth_sae(struct hostapd_data *hapd, struct sta_info *sta,
 			goto remove_sta;
 		if (sta->sae->state >= SAE_CONFIRMED ||
 		    !(hapd->conf->mesh & MESH_ENABLED)) {
+			int ret;
 			const u8 *var;
 			size_t var_len;
 			u16 peer_send_confirm;
@@ -1680,7 +1681,19 @@ static void handle_auth_sae(struct hostapd_data *hapd, struct sta_info *sta,
 				return;
 			}
 
-			if (sae_check_confirm(sta->sae, var, var_len) < 0) {
+			ret = sae_check_confirm(sta->sae, var, var_len);
+			if (ret < 0) {
+				if (ret == SAE_KEY_MISMATCH) {
+					u8 notify_mac[ETH_ALEN];
+#ifdef CONFIG_MTK_IEEE80211BE
+					if (sta->sae->dot11MultiLinkActivated)
+						os_memcpy(notify_mac, sta->sae->peer_ml_ie->ml_addr, ETH_ALEN);
+					else
+#endif
+					os_memcpy(notify_mac, sta->addr, ETH_ALEN);
+					wpa_msg(hapd->msg_ctx, MSG_INFO, AP_STA_POSSIBLE_SAE_PSK_MISMATCH MACSTR, MAC2STR(notify_mac));
+					hostapd_ubus_notify(hapd, "key-mismatch", notify_mac);
+				}
 				resp = WLAN_STATUS_UNSPECIFIED_FAILURE;
 				goto reply;
 			}
@@ -2654,6 +2667,17 @@ static int pasn_wd_handle_sae_confirm(struct hostapd_data *hapd,
 
 	res = sae_check_confirm(&pasn->sae, data + 6, buf_len - 6);
 	if (res != WLAN_STATUS_SUCCESS) {
+		if (res == SAE_KEY_MISMATCH) {
+			u8 notify_mac[ETH_ALEN];
+#ifdef CONFIG_MTK_IEEE80211BE
+			if (pasn->sae->dot11MultiLinkActivated)
+				os_memcpy(notify_mac, pasn->sae->peer_ml_ie->ml_addr, ETH_ALEN);
+			else
+#endif
+			os_memcpy(notify_mac, sta->addr, ETH_ALEN);
+			wpa_msg(hapd->msg_ctx, MSG_INFO, AP_STA_POSSIBLE_SAE_PSK_MISMATCH MACSTR, MAC2STR(notify_mac));
+			hostapd_ubus_notify(hapd, "key-mismatch", notify_mac);
+			}
 		wpa_printf(MSG_DEBUG, "PASN: SAE failed checking confirm");
 		return -1;
 	}
diff --git a/src/common/sae.c b/src/common/sae.c
index 6771e3c..02f525d 100644
--- a/src/common/sae.c
+++ b/src/common/sae.c
@@ -2461,7 +2461,7 @@ int sae_check_confirm(struct sae_data *sae, const u8 *data, size_t len)
 			    data + 2, hash_len);
 		wpa_hexdump(MSG_DEBUG, "SAE: Calculated verifier",
 			    verifier, hash_len);
-		return -1;
+		return SAE_KEY_MISMATCH;
 	}
 
 #ifdef CONFIG_SAE_PK
diff --git a/src/common/sae.h b/src/common/sae.h
index 2692380..4e30b68 100644
--- a/src/common/sae.h
+++ b/src/common/sae.h
@@ -23,6 +23,7 @@
 #define SAE_CONFIRM_MAX_LEN (2 + SAE_MAX_HASH_LEN)
 #endif /* CONFIG_SAE_PK */
 #define SAE_PK_M_LEN 16
+#define SAE_KEY_MISMATCH -2
 
 /* Special value returned by sae_parse_commit() */
 #define SAE_SILENTLY_DISCARD 65535
diff --git a/src/common/wpa_ctrl.h b/src/common/wpa_ctrl.h
index 0b92094..1ba305f 100644
--- a/src/common/wpa_ctrl.h
+++ b/src/common/wpa_ctrl.h
@@ -348,6 +348,7 @@ extern "C" {
 #define AP_STA_CONNECTED "AP-STA-CONNECTED "
 #define AP_STA_DISCONNECTED "AP-STA-DISCONNECTED "
 #define AP_STA_POSSIBLE_PSK_MISMATCH "AP-STA-POSSIBLE-PSK-MISMATCH "
+#define AP_STA_POSSIBLE_SAE_PSK_MISMATCH "AP-STA-POSSIBLE-SAE-PSK-MISMATCH "
 #define AP_STA_POLL_OK "AP-STA-POLL-OK "
 
 #define AP_REJECTED_MAX_STA "AP-REJECTED-MAX-STA "
